<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullScreen" parent="android:Theme.Holo.Light.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <!-- My Style -->
    <style name="LayoutHorizontalPanel">
        <item name="android:paddingBottom">@dimen/space_size_16</item>
        <item name="android:paddingTop">@dimen/space_size_16</item>
        <item name="android:paddingLeft">@dimen/space_size_8</item>
        <item name="android:paddingRight">@dimen/space_size_8</item>
        <item name="android:layout_marginBottom">@dimen/space_size_8</item>
        <item name="android:background">@color/gray_light_transparent</item>
    </style>

    <style name="LayoutVerticalPanel">
        <item name="android:paddingLeft">@dimen/space_size_16</item>
        <item name="android:paddingRight">@dimen/space_size_16</item>
        <item name="android:paddingTop">@dimen/space_size_8</item>
        <item name="android:paddingBottom">@dimen/space_size_8</item>
        <item name="android:layout_marginRight">@dimen/space_size_8</item>
        <item name="android:background">@color/gray_light_transparent</item>
    </style>

</resources>
